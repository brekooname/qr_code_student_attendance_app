import 'package:flutter/material.dart';
class DeleteAttendee extends StatelessWidget {
  const DeleteAttendee({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Delete Data'),
        centerTitle: true,
      ),
    );
  }
}
